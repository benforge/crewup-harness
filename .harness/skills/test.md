# 测试技能 SOP

当 agent 需要验证行为或回归风险时使用本 SOP。

## 步骤

1. 阅读 `requirement.md`，识别验收标准。
2. 优先运行与影响范围最贴近的测试。
3. 当变更触及共享包、API 契约、认证、数据或路由时，再扩大测试范围。
4. 跳过测试时必须记录具体原因。
5. 能对应验收标准的测试结果，尽量回链到对应验收项。

## 默认命令

```bash
npm run test
```
