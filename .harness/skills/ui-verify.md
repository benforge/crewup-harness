# UI 验证技能 SOP

当前端视觉或交互发生变化时使用本 SOP。

## 步骤

1. 从需求中识别受影响路由和需要覆盖的视口尺寸。
2. 如果应用需要 dev server，先启动相关服务。
3. 在浏览器里验证主要用户路径。
4. 用户可见 UI 发生变化时，检查桌面端和移动端布局。
5. 在 `artifacts/test-report.md` 记录截图、路由、视口尺寸和视觉风险。

## 证据要求

如果只写“看起来没问题”，但没有路由、视口或截图证据，则 UI 验证不完整。
